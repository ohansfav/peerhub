# Two Issues FIXED ✅

**1. Signup now auto-logs in → dashboard** (isOnboarded: true)
**2. Tutor onboarding subjects crash fixed** (handles empty/null subjects)

**Test:**
1. Signup new user → **dashboard (no login redirect)**
2. Tutor onboarding step 3 → **no DB error**

Restart server: `npm run dev`
